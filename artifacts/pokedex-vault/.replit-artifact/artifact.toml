kind = "web"
previewPath = "/"
title = "PokéVault — Card Collection Tracker"
version = "1.0.0"
id = "artifacts/pokedex-vault"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 26020

[services.development]
run = "pnpm --filter @workspace/pokedex-vault run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/pokedex-vault", "run", "build" ]
publicDir = "artifacts/pokedex-vault/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "26020"
BASE_PATH = "/"
