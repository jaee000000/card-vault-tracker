[phases.build]
cmds = ["cd artifacts/api-server && pnpm install", "cd artifacts/api-server && pnpm run build"]

[phases.start]
cmds = ["cd artifacts/api-server && pnpm run start"]

[start]
cmd = "cd artifacts/api-server && pnpm run start"
